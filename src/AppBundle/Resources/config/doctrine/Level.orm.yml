AppBundle\Entity\Level:
  type: entity
  table: level
  id:
    id:
      type: integer
      generator: { strategy: AUTO }
  fields:
    name:
      type: string
      length: 255
  oneToMany:
    lessons:
      targetEntity: Lesson
      mappedBy: level
